# Luna Pi Requirements
# For running Luna voice assistant on Raspberry Pi

# Core pipecat (install from parent directory)
# pip install -e ..

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# Audio I/O
PyAudio>=0.2.14

# AI Services (API-based, no local models)
anthropic>=0.39.0
openai>=1.50.0

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0
aiohttp>=3.9.0
pytz>=2024.1

# VAD
onnxruntime>=1.16.0

# Optional: Camera support with face/hand detection
# opencv-python>=4.8.0  # Uncomment for camera
# mediapipe>=0.10.0     # Uncomment for detection (may need mediapipe-rpi4 on Pi)
# picamera2>=0.3.12     # Uncomment for Pi Camera module

# Optional: WebRTC client mode (connects to remote server)
# aiortc>=1.9.0
# av>=12.0.0
websockets>=12.0

# Web server
uvicorn>=0.30.0
fastapi>=0.111.0
